"""eaccode — self-improving generalist agent (Hermes × Claude Code hybrid).

Bring your own keys: runs locally on Windows, Linux and macOS, works with any
model provider via LiteLLM, and learns from experience through persistent
memory and a skill loop.
"""

__version__ = "0.0.1"
